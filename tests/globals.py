# hey look it's a testing script lol ### #

if True:
  y = 4
else:
  x = 3

z = -8  # this is imported in other modules

a_bunch_of_math_functions = (
  (lambda x: x),
  (lambda x: -x),
  (lambda x: x + 1),
  (lambda x: x - 2),
  (lambda x: x * 4),
  (lambda x: x / 8),
  (lambda x: 0),
  (lambda x: x ** 2),
  (lambda x: 1 / x),
  (lambda x: 1 << x),
  (lambda x: x > 5), # not a typo
)
