if True:
  y = 4
else:
  x = 3

z = -8

identity_function = lambda x: x
negative = lambda x: -x
add_one = lambda x: x + 1
subtract_two = lambda x: x - 2
multiply_by_four = lambda x: x * 4
divide_by_eight = lambda x: x / 8
zero = lambda x: 0
square = lambda x: x ** 2
inverse = lambda x: 1 / x
shift_left = lambda x: 1 << x
is_greater_than_five = lambda x: x > 5

def get_z():
    return z
